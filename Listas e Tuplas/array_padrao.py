import numpy as np

numeros = np([1, 3.5])

import array as arr

arr.array('d',[1, 3.5] )
        # precisamos definir qual a tipagem desse array e depois inserir uma lista com os elementos desejados
       